#category-new
  %h2 Add a category
  = simple_form_for @category, url: admin_categories_path do |f|
    = f.input :name, required: false, autofocus: true
    .form-actions
      = f.button :submit, 'Add'