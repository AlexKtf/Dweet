#playlist-new
  %h2 Add a playlist
  = simple_form_for @playlist, url: admin_playlists_path do |f|
    = f.input :url, label: 'key', required: false
    = f.input :category_id, as: :hidden, input_html: { value: @category.id }
    .form-actions
      = f.button :submit, 'Add'